#include "monty.h"

/**
 * op_stack - function to on stack
 * @head: pointer to stack head
 * @line_number: line number
 *
 * Return: void
 */
void op_stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	glob.q_flag = 0;
}
